const User = require("./User");
const MovieReview = require("./MovieReview");
const OneRate = require("./OneRate");
const MovieComment = require("./MovieComment");

module.exports = { User, MovieReview, OneRate, MovieComment };
